second feature

this second feature shows the location of the user.

it is applied by assigning a constant and values for the location, as shown below.
const userLocation = user.location ? `<p>${user.location}</p>` : "";

then, using it and assigning its location, as shown below.
<p><strong>Location: </strong>${user.location}